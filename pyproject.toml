[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "govrail"
dynamic = ["version"]
description = "A language-agnostic governance plane for agent-driven development: gates and notes, delivered by the gov CLI."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "govrail" }]
keywords = ["agent", "governance", "gates", "notes", "development"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
dependencies = []

[project.urls]
Homepage = "https://github.com/Lixiang9716/govrail"
Repository = "https://github.com/Lixiang9716/govrail"
Issues = "https://github.com/Lixiang9716/govrail/issues"
Changelog = "https://github.com/Lixiang9716/govrail/blob/master/CHANGELOG.md"

[project.scripts]
gov = "gov.cli:main"

[project.optional-dependencies]
dev = ["pytest", "build"]

[tool.setuptools.dynamic]
version = { attr = "gov.__version__" }

[tool.setuptools.packages.find]
include = ["gov*"]

[tool.setuptools.package-data]
"gov" = ["HIGHLIGHTS.md"]
"gov.templates" = ["*", "skills/*/SKILL.md",
                   "presets/*/preset.json", "presets/*/README.md",
                   "presets/*/skills/*/SKILL.md"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
