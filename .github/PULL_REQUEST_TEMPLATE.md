## Summary

What this PR changes and why.

## Notes

- [ ] Every non-trivial change has an Agent Note in this PR.
- [ ] `gov run --mode all` passes.
- [ ] Bilingual docs (if touched) are re-recorded with `gov verify-pairing --write`.

## Verification

```sh
# commands run and their outcome
```
